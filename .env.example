# OPTIONAL. The deployed demo runs entirely from recorded runs and needs no key.
# Set this only if you want to enable Live mode (real API calls).
ANTHROPIC_API_KEY=

# Set to "1" to allow live runs on a public deployment. Defaults to off so a
# public URL cannot drain the key. See DECISIONS.md #12.
ALLOW_LIVE_RUNS=
